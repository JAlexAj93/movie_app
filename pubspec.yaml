name: movie_app
description: "Movie App Kueski"
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6        #Gestion del estado con cubit y bloc
  http: ^1.2.1                #Servicio para peticiones http
  connectivity_plus: ^6.1.0   #Servicio para detectar el estado de la conectividad del dispositivo

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:

  uses-material-design: true